# 改善計画と実施結果（2026年9月）

本ドキュメントは、公開中の [境町 ⇄ 東京 高速バス ミニ](https://sakaimachi-bus.amida-des.com/) を
「本当に価値のある道具」にするために立てた改善計画と、その実施・評価の記録です。

---

## 1. このアプリが解くべき課題

### 1.1 ユーザーの 3 つの状況

| # | 状況 | 知りたいこと | 求められる体験 |
|---|------|--------------|----------------|
| A | **直前** — バス停へ向かう / 向かおうとしている | 次に乗れる便は何時か。今から出て間に合うか | 入力ゼロ・1秒で判断できる |
| B | **計画** — 明日・週末の予定を立てている | 始発と最終、1日の本数、その日のダイヤ種別 | 全便が一覧でき、日付を変えられる |
| C | **初回** — 初めてこの路線に乗る | のりばはどこか、運賃はいくらか、支払い方法は | 迷いどころが先回りで説明されている |

### 1.2 競合・類似サービスとの比較

| サービス | 強み | このアプリが勝てる点 |
|----------|------|----------------------|
| NAVITIME / ジョルダン / 駅すぱあと | 全国網羅・乗換検索・リアルタイム | 検索操作が必要。広告が多い。1路線に最適化されていない |
| 関東鉄道 / JRバス関東 公式 | 一次情報で最も正確 | 表組みが重く、スマホで「次発」を読み取りにくい |
| 境町公式サイト | 地域の一次情報 | 時刻表が PDF。開くまでが遠い |
| Google マップ | door-to-door の経路探索 | 毎回入力が必要。1日の全便を俯瞰しづらい |

→ **差別化は「この 1 路線に特化した、入力ゼロ・即時・オフライン・広告なし」**。
だからこそ **正確性・即時性・データ鮮度の透明性** が生命線になる。

---

## 2. 現状の課題（実測にもとづく）

公開ビルドを実際に動かし、Playwright / Lighthouse / Pa11y で計測して洗い出した。

### Critical（誤情報・機能不全）

| # | 課題 | 影響 |
|---|------|------|
| C1 | 「表示基準時刻」を localStorage に永続化していた | **2 回目以降の訪問で初回訪問時の時刻が基準になり、次発が恒常的に誤表示される** |
| C2 | 祝日カレンダー未対応（曜日のみで判定） | 祝日に平日ダイヤを表示。例: 2026-09-21 敬老の日、2026-09-22 国民の休日 |
| C3 | Service Worker の登録パスが `'{serviceWorkerPath}'` という文字列リテラルのまま（Astro の `is:inline` は式を展開しない） | **PWA / オフライン機能が一切動作していない**（404） |
| C4 | 全ページで React のハイドレーション不整合（#418 / #423 / #425） | SSR した HTML を破棄して全再描画。CLS 悪化・無駄な処理 |
| C5 | 最終便を過ぎると「本日の運行は終了しました」で行き止まり | 夜間に使うと何も得られない |

### High（価値不足）

| # | 課題 |
|---|------|
| H1 | 全便一覧がない（「以降」4 件のみ）。日付も変えられない |
| H2 | 1 秒更新のカウントダウンに `aria-live="polite"` → スクリーンリーダーが毎秒読み上げ |
| H3 | README は「ダークモード対応」と書いているが CSS に実装がない |
| H4 | データの改定日が本文中にあるだけ。古くなっても気づけない |
| H5 | 運賃・のりばがトップから遠い。運賃額は改定済みで実態と合わない可能性 |
| H6 | 「間に合うか」の判断材料がない（残り時間の数字だけ） |

### Medium（品質）

| # | 課題 |
|---|------|
| M1 | `05時間35分後` のゼロ埋め表記 |
| M2 | デスクトップが 1 カラムで余白過多 |
| M3 | ページ間で `<head>` とナビが重複し、内容も不統一 |
| M4 | OG 画像が全ページ共通 |
| M5 | ガイドが外部画像（Unsplash）と Google Maps iframe に依存 |
| M6 | ページごとの `<h1>` が同一で SEO 上の差別化がない |

---

## 3. 改善計画

### P0 — 正確性（ここが崩れると他のすべてが無意味）
- [x] 基準時刻を永続化しない。常にリアルタイム、手動指定は URL パラメータ限り
- [x] 日本の祝日エンジンを内蔵（振替休日・国民の休日・春分/秋分・五輪特例）
- [x] 最終便後は翌日以降の始発へ自動で繰り越す
- [x] Service Worker を `define:vars` で正しく登録し、オフライン動作を実現
- [x] ハイドレーション不整合を構造的に解消（時刻依存の描画は必ずマウント後）

### P1 — 中核体験
- [x] その日の**全便**を一覧表示し、次発をハイライト。発車済みは減光
- [x] 今日 / 明日 / 明後日 / 任意の日付の切り替え
- [x] 「バス停まで徒歩◯分」から **間に合う / そろそろ / 急いで** を判定
- [x] 乗車停留所（東京駅 / 王子駅）を選ぶと発車時刻がその停留所基準に変わる
- [x] 便を選んで **カレンダー（.ics）に追加**。徒歩時間＋5分前にアラーム
- [x] 経路を「発 → 経由 → 着」のタイムラインで表示。所要時間とのりばを併記

### P2 — 信頼性・データ鮮度
- [x] 時刻表データに `revision` / `verifiedOn` / `sources` / `pendingRevision` を持たせる
- [x] 未反映の改定があるときは全ページ上部で告知し、一次情報へ誘導
- [x] `npm run validate:timetable` でスキーマ・時刻の逆転・発車順を機械検証（CI 必須）
- [x] 検証できない運賃額は掲載せず、公式ページへ明示的に誘導

### P3 — 体験の質
- [x] ダークモード（OS 追従 + 手動切替、描画前適用でフラッシュなし）
- [x] カウントダウンの `aria-live` を廃し、要約を静的に読み上げ可能にする
- [x] タブ非表示中はタイマーを止めて電池を使わない
- [x] デスクトップ 2 カラム、モバイル 2 段ヘッダー
- [x] ページ別 OG 画像、共通レイアウト、`/timetable/` の新設
- [x] dayjs を廃し `Intl` ベースの自前実装へ（依存とバンドル削減）

### P4 — 検証の自動化
- [x] ロジックのユニットテスト（`node --test`）
- [x] ブラウザ実機の回帰テスト（Playwright）
- [x] Lighthouse / Pa11y を CI で必須化

---

## 4. 実施結果の評価

### 4.1 自動計測

| 指標 | Before | After |
|------|--------|-------|
| Lighthouse Performance | 0.81 | **0.99 – 1.00** |
| Lighthouse Accessibility | 0.96 | **1.00** |
| Lighthouse Best Practices | 0.96 | **1.00** |
| Lighthouse SEO | 1.00 | **1.00** |
| Cumulative Layout Shift | 0.377 | **0.001 – 0.036** |
| コンソールエラー | 全ページで発生（React #418/#423/#425、SW 404） | **0** |
| Pa11y (WCAG 2.1 AA) | — | **6/6 ページ 0 errors** |
| ユニットテスト | なし | **29 件パス** |
| ブラウザ回帰テスト | なし | **19 件パス** |

### 4.2 計画項目の達成状況

P0〜P4 のすべてにチェックが入っている。特に検証したのは次の 3 点。

1. **C1 の回帰テスト** — 旧キー `sbm:user-preferences` から移行したとき、保存済みの `nowValue` が
   確実に破棄されることをブラウザ実機で確認。
2. **C2 の正しさ** — 2020〜2027 年の祝日をユニットテストで固定。
   2026-09-21（敬老の日）・2026-09-22（国民の休日）で土日祝ダイヤ、
   2026-09-24（木）で平日ダイヤになることを実機で確認。
3. **C3 のオフライン動作** — Service Worker が `active` になり、
   ネットワークを切断した状態でもページが開いて次発が表示されることを確認。

### 4.3 残課題

- **時刻表データそのものの更新**: 2026年1月1日のダイヤ・運賃改定が告知されているが、
  一次情報（公式 PDF / 事業者サイト）を参照できない環境で作業したため、**時刻は変更していない**。
  誤った時刻を載せるより、改定の存在を明示して一次情報へ誘導するほうが安全という判断。
  `src/data/timetable.json` の `meta.pendingRevision` を消し、`revision` / `revisionLabel` /
  `verifiedOn` を更新すれば告知バナーは自動的に消える。
- 遅延・運休のリアルタイム表示（一次データソースが必要）
- 英語版（訪日観光客向け）

---

## 5. データを更新する手順

```bash
# 1. 公式時刻表と突き合わせて src/data/timetable.json を編集する
#    - meta.revision / meta.revisionLabel を新しい改定に合わせる
#    - meta.verifiedOn に確認した日 (YYYY-MM-DD) を入れる
#    - 反映が済んだら meta.pendingRevision を null にする
# 2. 機械検証（時刻の逆転・発車順・必須項目をチェック）
npm run validate:timetable
# 3. ロジック・型・ビルドをまとめて確認
npm run verify
```
